% Test if output directory works.
\usemodule[filter]

\defineexternalfilter 
  [markdownA]
  [filter={pandoc -w context -o \externalfilteroutputfile },
   directory=temp/] % Trailing /

\defineexternalfilter 
  [markdownB]
  [filter={pandoc -w context -o \externalfilteroutputfile },
   directory=temp] % No trailing /

\defineexternalfilter 
  [markdownC]
  [filter={pandoc -w context -o \externalfilteroutputfile },
   directory=../output] % parent directory


\traceexternalfilters

\starttext

\startmarkdownA
## Lists ##

Unordered (bulleted) lists use asterisks, pluses, and hyphens (`*`,
`+`, and `-`) as list markers. These three markers are
interchangeable. As an example, this:


    *   Candy.
    *   Gum.
    *   Booze.

gives:

*   Candy.
*   Gum.
*   Booze.
* This is a list
* that has **markdown** syntax
* does it _work_
\stopmarkdownA

\startmarkdownB
  And  now for something completely different. Does **Bold** work?
\stopmarkdownB

\startmarkdownC
  And  now for something completely different. Does *italic* work?
\stopmarkdownC

\stoptext
\endinput

